import 'package:flutter/material.dart';

/// Application colors.
class AppColors {
  AppColors._();

  /// Primary light color.
  static const Color lightPrimaryColor = Color(0xFF34A9FF);

  /// Accent light color.
  static const Color lightAccentColor = Color(0xFF808080);

  /// Primary dark color.
  static const Color darkPrimaryColor = Colors.pink;

  /// Accent dark color.
  static const Color darkAccentColor = Colors.lightBlueAccent;

  /// Facebook primary color.
  static const Color facebookPrimaryColor = Color(0xFF4267B2);

  /// Edit text field border color.
  static const Color editTextFieldBorderColor = Color(0xFFE0E0E0);

  /// Edit text field title color.
  static const Color editTextFieldTitleColor = Color(0xFF424242);

  /// Button shadow color.
  static const Color buttonShadowColor1 = Color.fromRGBO(52, 169, 255, 0.2);

  /// Button shadow color.
  static const Color buttonShadowColor2 = Color.fromRGBO(52, 169, 255, 0.12);

  /// Button shadow color.
  static const Color buttonShadowColor3 = Color.fromRGBO(0, 129, 225, 0.14);
}

